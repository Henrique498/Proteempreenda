# requirements.txt — Proteempreenda / GuardianNet backend
#
# Versões FIXADAS a partir do requirements.txt original (sem pins) do
# projeto, resolvidas hoje (03/09/2026) — confirmadas contra o log real
# do Render: "Starting gunicorn 26.2.0" bate exatamente com o que foi
# resolvido aqui, então este é o snapshot real que está rodando lá.
#
# Por que fixar: o requirements.txt original usava "river>=0.21.0" (sem
# teto), então cada novo build no Render podia puxar uma versão mais nova
# do River — foi isso que quebrou o modelo salvo no Supabase (pickle
# feito com uma versão antiga do Pipeline, incompatível com a nova).
# Fixar todas as versões garante builds reprodutíveis: o que funciona
# hoje continua funcionando amanhã, até você decidir atualizar de propósito.
#
# deep-translator REMOVIDO: o modelo (treinar_pan12.py) foi treinado
# direto em PT-BR, sem tradução. Traduzir o texto pra EN antes de
# aprender/prever fazia o modelo tratar tudo como vocabulário
# desconhecido — essa era a causa real do bug de aprendizado que não
# "pegava". Ver ia_service.py atualizado.

Flask==3.1.3
Flask-Cors==6.0.5
psycopg2-binary==2.9.12
python-dotenv==1.2.3
Werkzeug==3.1.8
gunicorn==26.2.0
river==0.26.1